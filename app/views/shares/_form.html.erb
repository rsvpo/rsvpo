<%= simple_form_for(@share) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :receiver %>
    <%= f.input :message %>
    <%= f.association :user %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
